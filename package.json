{
  "name": "miz",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "start": "bun run dependencies:update -- normal --display-tool-versions --startup && bun run prisma:generate && bun run scripts/start.ts normal",
    "start:docker": "bun run dependencies:update -- docker --display-tool-versions --startup && bun run prisma:generate && bun run scripts/start.ts docker",
    "dev": "bun run dependencies:update -- normal --display-tool-versions --startup && bun run prisma:generate && bun --watch scripts/start.ts normal",
    "dev:docker": "bun run dependencies:update -- docker --display-tool-versions --startup && bun run prisma:generate && bun --watch scripts/start.ts docker",
    "dependencies:update": "bun run scripts/update-dependencies.ts",
    "test": "bun test",
    "typecheck": "bun run prisma:generate && tsc --noEmit",
    "prisma:generate": "bun node_modules/prisma/build/index.js generate",
    "prisma:push": "bun node_modules/prisma/build/index.js db push",
    "prisma:migrate": "bun run scripts/prisma-migrate.ts"
  },
  "dependencies": {
    "@naplink/naplink": "1.1.5",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dayjs": "^1.11.23",
    "fast-xml-parser": "^5.11.1",
    "jimp": "^1.6.1",
    "node-cron": "^4.6.0",
    "pg": "^8.23.0",
    "prisma": "^7.9.1",
    "qrcode": "^1.5.4",
    "qrcode-reader": "^1.0.4",
    "winston": "^3.19.0",
    "zod": "^4.5.1"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "@types/qrcode": "^1.5.6",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "@hono/node-server": "1.19.13"
  }
}
